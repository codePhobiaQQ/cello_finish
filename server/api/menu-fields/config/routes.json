{
  "routes": [
    {
      "method": "GET",
      "path": "/menu-fields",
      "handler": "menu-fields.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/menu-fields",
      "handler": "menu-fields.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/menu-fields",
      "handler": "menu-fields.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
